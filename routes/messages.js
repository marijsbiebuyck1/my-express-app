import express from 'express';

const router = express.Router();

// CLEAN SCAFFOLD — start here from scratch
// TODO: implement message endpoints below, one by one.

// Example endpoints to implement when ready:
// GET    /messages         -> list messages
// POST   /messages         -> create message
// GET    /messages/:id     -> get single message
// PUT    /messages/:id     -> replace message
// PATCH  /messages/:id     -> partial update
// DELETE /messages/:id     -> delete message

// For now the router is intentionally empty so you can implement each route afresh.

export default router;


